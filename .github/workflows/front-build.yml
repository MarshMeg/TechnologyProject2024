name: frontend build

on: push

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: install dependencies
        working-directory: ./frontend
        run: npm i
      - name: build
        working-directory: ./frontend
        run: npm run build
